<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.test4.toolbar.MainActivity">
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never" />

    <item android:id="@+id/my_action"
        android:icon="@drawable/wifi"
        android:title="My Action"
        android:showAsAction="ifRoom|collapseActionView"
        android:actionLayout="@layout/my_action" />
    <item android:id="@+id/myview"
        android:icon="@drawable/ic_action"
        android:title="hrllo"
        android:showAsAction="ifRoom|collapseActionView"></item>
</menu>
